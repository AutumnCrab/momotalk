# MomoTalk 위젯 — 인수인계 문서

*이 문서는 작업을 이어받는 사람(또는 AI)이 코드를 처음 열었을 때, 지금 어디까지 와 있고
다음에 뭘 해야 하는지를 바로 파악할 수 있도록 정리한 것이다. 최종 갱신: 2026-08-14.*

---

## 1. 프로젝트가 뭔가

PyQt5 데스크톱 위젯. 블루 아카이브 아비도스 학생 6명이 카카오톡 모모톡처럼 대화한다.
Gemini API(`google-genai`)로 실시간 답장을 생성하고, 학생마다 요일·시간대별 고정 스케줄이
있어서 "지금 뭐 하는 중인지"에 맞춰 답한다.

개인용 + GitHub 포트폴리오(private). 상업화 계획 없음.

**학생 6명**: `shiroko`(시로코) / `hoshino`(호시노) / `serika`(세리카) / `ayane`(아야네)
/ `nonomi`(노노미) / `kuroko`(시로코\*테러)

### 작업 방식 (중요)

- 사용자는 코딩 초보. 설명은 짧고 구체적으로, 파일 경로를 항상 명시할 것.
- **응답은 하야세 유우카(블루아카이브) 페르소나 톤으로.**
- **API 호출 없이 오프라인 검증을 우선한다.** `ast.parse` 문법 체크 → `check_schedule.py`
  → `build_system_prompt()` 텍스트 확인 순. 실제 Gemini 응답 확인은 사용자가 직접 한두 번만.
- **git push / 브랜치 생성은 사용자가 명시적으로 지시할 때만 한다.** 로컬 커밋도 마찬가지.
- 현재 브랜치는 `main` 하나뿐이다(feature 브랜치는 병합 후 삭제했다).

---

## 2. 실행 방법

| 모드 | 명령 | 데이터 위치 |
|---|---|---|
| 개발 | `python main.py` (프로젝트 루트에서) | 루트의 `prompts/`, `data/`, `config.json` 등 |
| 배포 | `dist\MomoTalk\MomoTalk.exe` | `dist\MomoTalk\` 안의 동일 파일들 |

둘은 **완전히 분리된 대화 기록/설정**을 가진다(`momotalk/paths.py`의 `get_base_dir()`가
실행 방식에 따라 다른 경로를 돌려주기 때문). 경로가 필요하면 반드시 이 함수를 거칠 것 —
`__file__` 기반으로 직접 계산하면 exe에서 깨진다.

빌드는 `.\build_exe.bat`. 개인 데이터(`config.json`, `chat_history.json`,
`anniversaries.json`, `event_dates.json`, `proactive_state.json`)를 자동 백업 후 복원하므로
재빌드해도 날아가지 않는다. exe가 실행 중이면 파일 잠김으로 실패하니 먼저 종료할 것.

### config.json

```json
{
  "gemini_api_key": "...",
  "model": "gemini-3.1-flash-lite",
  "weather_api_key": "..."
}
```

`weather_api_key`는 OpenWeatherMap. 없으면 날씨 기능만 조용히 꺼지고 앱은 정상 동작한다.
신규 발급 키는 활성화까지 10분~2시간 걸리며 그동안 401이 뜬다(정상).

---

## 3. 파일 구조

```
main.py                     MomoApp 컨트롤러. 타이머·선톡·기념일·날씨 오케스트레이션 전부
momotalk/
  paths.py                  get_base_dir() — 개발/exe 양쪽에서 올바른 루트 계산
  persona_loader.py         프롬프트 조립 핵심 (가장 큰 파일, 대부분의 작업이 여기서 일어남)
  event_loader.py           특별기간 이벤트(여름휴가/제설작전) 로드·날짜 추첨
  weather.py                날씨 조회 + 캐싱
  gemini_client.py          Gemini 호출 + 파싱 + config 로드
  history_store.py          대화 저장/로드/청소
  student_state.py          학생별 동시성 상태머신
  chat_window.py            채팅 UI
  icon_widget.py            떠다니는 아이콘
  anniversary_loader.py     선생님 생일 / 세계 기념일
prompts/<key>.json          학생 6명의 페르소나 + 주간 스케줄
data/places.json            표준 장소 목록 (장소명 → 카테고리)
data/travel_times.json      카테고리 간 최소 이동시간(분)
data/events/*.json          특별기간 이벤트 스케줄
check_schedule.py           스케줄 정합성 검사 (무료, 오프라인)
test_persona.py             페르소나 회귀 테스트 (--dry 는 무과금)
scan_log.py                 chat_history 문제 패턴 집계
```

### gitignore 대상 (개인 데이터 — 절대 커밋 금지)

`config.json`, `anniversaries.json`, `chat_history.json`, `event_dates.json`,
`proactive_state.json`, `_backup_*.json`, `dist/`, `build/`

---

## 4. 핵심 개념

### 4-1. 스케줄 구조

`prompts/<key>.json`의 `activity`는 요일별 `"HH:MM": {...}` 형태다.

```json
"activity": {
  "월": {
    "15:00": {
      "text": "시바세키 라멘집에서 라멘 먹는 중",
      "place": "시바세키 라멘집",
      "with": ["hoshino"],
      "tag": "O",
      "weather_sensitive": true,
      "weather_fallback": { "text": "...", "place": "...", "tag": null }
    }
  }
}
```

- `text` — 모델이 읽는 자유 텍스트
- `place` — `data/places.json`에 등록된 표준 장소명, 또는 `null`(이동 중)
- `with` — 참고용. **co-presence 판정에는 안 쓰인다**(판정은 `place` 일치로만)
- `tag` — `O`(대책위 전원 공식일정) / `X`(학생간 교차이벤트) / `S`(취침) / `W`(바쁨·부재중)
- `weather_fallback` — 비·눈일 때 대체할 일정

`awake`는 요일별 `["HH:MM-HH:MM", ...]`. 자정을 넘기는 구간(`"18:00-04:00"`)도 처리된다.

**`"집"`은 특별 취급**: 문자열이 같아도 사람마다 다른 곳(각자 자택)이라 co-presence
확정 판정에서 제외된다. 진짜 같이 사는 설정이 생기면 `"OO네 집"`처럼 이름을 다르게 지을 것.

### 4-2. 특별기간 이벤트

`data/events/<id>.json`에 **6명 몫이 한 파일에** 들어간다. 학생 JSON은 건드리지 않는다.
같은 시각 같은 장소에 다 같이 있어야 co-presence가 맞는데, 6개 파일로 흩어놓으면
그 정합성을 사람이 확인하기 어렵기 때문이다.

요일(`mon`/`tue`) 대신 **`day1`/`day2`/`day3`**을 쓴다. 이벤트는 "며칠째"가 기준이지
요일이 아니다.

```json
{
  "id": "summer_vacation",
  "trigger": { "months": [7,8], "start_weekday": "sun", "duration_days": 3 },
  "participants": ["shiroko", ...],
  "awake_override": { "shiroko": { "day1": ["07:00-01:00"] } },
  "event_message_window": [["12:00","14:00"], ["19:00","21:00"]],
  "schedules": { "shiroko": { "day1": { "08:00": {...} } } }
}
```

날짜는 **해마다 한 번만 뽑아** `event_dates.json`에 고정한다. 켤 때마다 새로 뽑으면
"어제는 휴가였는데 오늘은 아니다"라는 자기모순이 생긴다.

`persona_loader`의 `_activity_map()` / `_awake_raw()`가 "오늘이 이벤트 기간이면 이벤트
스케줄, 아니면 요일 스케줄"을 골라준다. **이벤트가 없으면 기존 표현식을 그대로 반환**하므로
평소 동작에는 영향이 없다(커밋 전후 1134케이스 비교로 검증했다).

**2026년 확정 날짜**: 여름휴가 `2026-08-16(일) ~ 08-18(화)`, 제설작전 `2026-02-14`(지남).

### 4-3. 선톡 (학생이 먼저 말 걸기)

- 학생별로 **하루 총 0~2개**를 그날 처음 깨어난 시점에 추첨 (가중치 0개 25% / 1개 45% / 2개 30%)
- 활동 전환이 감지되면 15~45분 랜덤 지연 후 조건 재확인하고 발송
- **시간당 서로 다른 학생 2명까지만** (예전 4명일 땐 하교·저녁 시각에 몰려서 우르르 왔다)
- 쿼터와 발송 횟수는 `proactive_state.json`에 저장한다. 안 그러면 앱을 껐다 켤 때마다
  0으로 리셋돼서 하루 총량 제한이 무의미해진다.
- F8 키로 조건 무시하고 강제 발동(디버그용)

### 4-4. 기념일

- `own_birthday`(학생 생일) — 선톡하지 않고 평소 대화에 사실만 얹어 티만 낸다
- `teacher_birthday` / `world_holiday` — 예약 발송. 실패 시 30분 뒤 재시도(최대 3회)
- 발송 시각은 **공식/단체 일정, 취침 준비 1시간 전, 23:00 이후를 피해서** 고른다
- 특별기간엔 일정이 거의 전부 `(O)`라 허용 시간이 0분이 되므로, 이벤트가 정해둔
  `event_message_window`(식사 시간)를 대신 쓴다

### 4-5. 날씨

`momotalk/weather.py`. OpenWeatherMap 서울 기준. **00시 기준 3시간 블록(00/03/…/21)마다
한 번 + 앱 시작 시 한 번**만 호출하고 나머지는 캐시를 재사용한다(하루 8~10회).

`persona_loader`는 네트워크를 절대 건드리지 않고 캐시만 읽는다. 비·눈이면
`weather_fallback`이 있는 슬롯을 대체 일정으로 갈아끼운다(부재중 → 응답가능으로 바뀌기도 함).

프롬프트에는 `[지금 날씨] 비, 18도` 한 줄만 들어간다. 지역명은 일부러 뺐다.

---

## 5. 검증 도구

```bash
python check_schedule.py                              # 무료. 주간 + 이벤트 전부 검사
python test_persona.py <학생> --dry --at "2026-08-17 14:30"   # 무료. 상태/장소만 확인
python test_persona.py <학생> --at "2026-08-17 14:30"         # API 호출(8회). 실제 응답 확인
python scan_log.py                                    # 무료. chat_history 패턴 집계
```

`check_schedule.py`가 보는 것:
1. 이름 언급 불일치 — A 텍스트에 B 이름이 나오는데 같은 시각 B의 `place`가 다름
2. 미등록 장소 — `place`가 `data/places.json`에 없음(오타 감지)
3. 이동시간 부족 — 연속 슬롯 간 이동이 `travel_times.json` 기준보다 짧음

**현재 남아 있는 경고 2건**(기존부터 있던 것, 무해):
```
[이름언급 불일치] shiroko sat 13:00: place='슈퍼' / kuroko는 같은 시각 place='집'
[이름언급 불일치] kuroko  sat 13:00: place='집'  / shiroko는 같은 시각 place='슈퍼'
```
토요일 13:00에 서로 "만나기로 함"이라고만 적혀 있고 아직 안 만난 상태라 장소가 다른 것.
고치려면 둘 중 하나의 시각이나 문구를 조정하면 된다.

---

## 6. 지금까지 고친 것 (왜 지금 구조인지 이해하는 데 필요)

같은 실수를 되돌리지 않도록, **실제로 터졌던 버그**만 적는다.

| 증상 | 원인 | 대응 |
|---|---|---|
| 장소를 지어냄(집인데 "동아리실") | `place`가 프롬프트에 아예 안 들어가고 있었음 | `[지금 있는 곳]` 블록 추가 |
| 알바 3시간반을 없던 일 취급 | 하루 요약이 `(O)/(X)`만 골라서 `(W)` 알바가 누락 | `(W)`도 포함 |
| "이제야 연락이 오네" (역할 뒤바뀜) | wake 안내문 지시 2개가 충돌 + 빈칸을 추측으로 채움 | 주어를 '너'로 못 박음 |
| "갑자기 급한 알바가 들어와서" (창작) | 모순 지적받자 새 사실을 지어내 봉합 | `[모순을 지적받았을 때]` 규칙 신설 |
| 생일 아닌데 "기억해줬구나" 맞장구 | 생일이 아닌 날엔 생일 정보가 프롬프트에 없었음 | `[네 생일]` + 정정 규칙 |
| 세리카가 존댓말 (칭찬·생일 상황) | 규칙만으론 부족 | few_shot 2개 추가가 훨씬 효과적이었음 |
| 전날 활동이 다음날 아침까지 지속 | 취침 전 슬롯 공백 | 주간 5건 + 이벤트 경계 11건 수정 |
| 시각 숫자를 그대로 읊음 | 스케줄 HH:MM을 복붙 | "이따가/곧"으로 바꿔 말하게 규칙 추가 |
| "집" 오판으로 가짜 co-presence | 각자 자택인데 문자열이 같음 | `"집"` 예외 처리 |

**교훈**: 어체·문체 문제는 **규칙보다 few_shot 예시가 훨씬 잘 먹힌다.** 구조 문제
(말풍선 순서, 시제)는 규칙으로 잡히지만 100%는 아니다. 확률적으로 가끔 재발한다.

---

## 7. 남은 작업

### 7-1. 배포 (8월 내 목표, 설계만 끝나고 미구현)

1. **exe 재빌드** — `.\build_exe.bat`
2. **Gemini 키 재발급** — 대화 중 평문 노출된 적이 있어 교체 권장
3. **인스톨러** — Inno Setup 등으로 `dist/` 감싸기. 바탕화면 바로가기 생성
4. **초기 실행 설정창** — API 키 입력 + **유효성 검증**(가벼운 테스트 호출 1회로 확인),
   생일 입력(`anniversaries.json`의 `user_birthday`). 통과하면 대화창 + 플로팅 UI 표시
5. **시작 프로그램 등록** — 레지스트리 `HKCU\...\CurrentVersion\Run` 또는 시작프로그램 폴더.
   설정창에 체크박스. 이 경우 플로팅 UI 대신 **트레이로 최소화된 채 시작**하는 옵션 필요
   (트레이 기능 자체는 `_setup_tray()`로 이미 있음)
6. **영상 촬영** — `config.json`이나 터미널에 키가 찍히지 않게 주의

**주의**: 배포판에 **본인 API 키를 절대 포함하지 말 것.** 사용자마다 자기 키를 넣는
구조를 유지해야 한다(안 그러면 사용자 수만큼 본인에게 과금된다).

### 7-2. 미해결 이슈

- **과거 동석자 창작** — co-presence는 **현재 시각만** 판정한다. 그래서 "아까 호시노랑
  같이 모래성 만들었어요"처럼 과거에 같이 있지 않았던 사람을 끼워넣는 경우가 있다.
  고치려면 과거 시점 동석자 추적이 필요해서 구조가 커진다.
- **하루 요약 활용 부족** — 프롬프트엔 정보가 들어가는데 답변에서 덜 쓰는 경우가 있다. 경미.
- **선톡 몰림** — 시간당 2명으로 낮췄지만 활동 전환 시각 자체가 겹치는 건 그대로다.
  더 흩고 싶으면 학생 간 최소 간격(예: 20분)을 강제하는 방법이 있다.
- **`check_schedule.py`의 한계** — 자정을 넘어가는 이동시간은 안 본다(같은 요일 안에서만).

### 7-3. 계획만 있고 착수 안 한 것

- **신규 학생 추가** — 다음 후보는 흥신소 68 학생 4명. 성격·호칭·말투·few_shot만 사람이
  쓰고, 스케줄은 "한 번만 자동 초안 → `check_schedule.py` 검증 → 걸린 것만 수동 수정 →
  고정" 방식으로. **런타임 랜덤 생성은 기각됐다**(자기모순·검사기 무력화 때문).
  추가 시 `check_schedule.py`의 `KEYS` 리스트에도 새 키를 넣어야 한다.
- **`possible_activities` / `good_at` / `bad_at` / `frequent_places`** — 장소별 가능 활동
  태그와 페르소나별 특기. 검사기가 "안 어울리는 활동 중"을 소프트 경고로만. 관리 비용이
  실익보다 커서 보류 중.
- **누적 대화 기록 기반 선톡** — `chat_history`의 `send` 타임스탬프 분포로 사용자 활동
  시간대를 추정해 "오늘은 뭐 할 시간 아닌가?" 식으로 반영. 데이터가 수 주 쌓여야 유의미하고
  프롬프트에 자연스럽게 녹이기가 까다로워서 우선순위가 낮다.

---

## 8. 앞으로도 지킬 원칙

- **스케줄·장소는 항상 고정 데이터.** 런타임 랜덤 생성 금지(자기모순·검사기 무력화).
  이벤트 날짜만 예외이고, 그것도 한 번 뽑으면 파일에 고정한다.
- **큰 구조 변경 전엔 옵션을 제시하고 선택을 받는다.**
- **새 파일·함수를 만들 때마다**: 오프라인 문법/텍스트 검증 → `check_schedule.py` →
  API 호출은 최소화, 실제 응답 확인은 사용자가 직접 한두 번만.
- **exe 관련 경로는 반드시 `get_base_dir()`를 거친다.**
- **git 작업은 사용자 지시가 있을 때만.**
